/**
 * Spring Security configuration. M0 holds only the baseline HTTP security posture
 * (which endpoints are public, stateless sessions, CSRF disabled). No JWT filter,
 * authentication provider, or user store yet — those arrive in M1/M2.
 */
package com.w3auth.backend.security;
