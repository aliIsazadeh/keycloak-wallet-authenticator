/**
 * Challenge domain: the {@code Challenge} model, {@code Nonce} generation,
 * {@code ChallengePolicy}, the SIWE message formatter, and the
 * {@code ChallengeStore} port. Pure core — must not depend on Spring or
 * infrastructure.
 */
package com.w3auth.backend.challenge;
