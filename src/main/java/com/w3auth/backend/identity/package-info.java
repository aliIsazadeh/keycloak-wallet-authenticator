/**
 * Identity domain: CAIP-10 account identity value objects ({@code Namespace},
 * {@code CaipAccountId}). Pure core — must not depend on Spring or infrastructure.
 */
package com.w3auth.backend.identity;
