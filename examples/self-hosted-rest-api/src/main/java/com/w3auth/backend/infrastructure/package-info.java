/**
 * Infrastructure adapters: Redis implementations of core ports, JPA entities and
 * repositories, Flyway migrations, and the Spring wiring for core beans. This is
 * the only place the core ports meet a concrete technology.
 */
package com.w3auth.backend.infrastructure;
